# План работы — сайт «ПРО Ремонт 76»

Источник задачи: `Клиенты/Дмитрий +79092800055 Ярославль ремонт/Промт.md`.

## Этапы

1. ~~Инициализация проекта: Astro + Tailwind CSS v4, TypeScript strict.~~
2. ~~Попытка спарсить реальные фото из группы ВК vk.ru/pro_remont76.~~ — не удалось: ВК отдаёт страницу входа без авторизации, автоматический доступ к контенту группы закрыт. Используем SVG-плейсхолдеры, реальные фото — через `CONTENT-TODO.md`.
3. ~~Дизайн-система: CSS-переменные (цвета, типографика, отступы), базовые компоненты (Button, Card, Section, Container), страница `/styleguide`.~~
4. ~~Сквозные компоненты: Header (липкая шапка), Footer, FloatingMessengers, Breadcrumbs.~~
5. ~~Главная страница целиком: оффер, три уровня ремонта, блок «до/после» (с компонентом сравнения фото), этапы работы, преимущества, отзывы, форма заявки.~~
6. ~~Стоп-точка — показать главную клиенту, дождаться правок.~~ Правок не поступило, продолжили без остановки по прямому запросу клиента.
7. ~~Остальные страницы: Услуги (+ 5 подстраниц по шаблону), Цены, Портфолио (+ 3 карточки проекта по шаблону), О компании, Контакты, Политика конфиденциальности, 404.~~
8. ~~Финальный проход: SEO-метатеги, JSON-LD (LocalBusiness, FAQPage, BreadcrumbList, ImageObject), sitemap.xml (авто через `@astrojs/sitemap`), robots.txt, README.md, CONTENT-TODO.md.~~

## Стек

Astro (static output) + Tailwind CSS v4 + ванильный TypeScript/JS для интерактива (слайдер до/после, форма, фильтры, лайтбокс). Обоснование — в `DECISIONS.md`.

## Статус

Все 8 этапов выполнены. Сайт полностью свёрстан: 7 разделов (главная, услуги + 5 подстраниц,
цены, портфолио + 3 карточки проекта, о компании, контакты) + политика конфиденциальности + 404,
итого 17 страниц. Дизайн-система выдержана единообразно на всех страницах.

Проверено: `npm run build` без ошибок, `npx astro check` — 0 ошибок/предупреждений, headless-браузерный
смоук-тест по всем 17 маршрутам (заголовки, `h1`, консольные ошибки, битые ссылки — не найдено),
плюс интерактив: слайдер до/после, фильтры портфолио, лайтбокс, формы (основная и мини) с
валидацией и honeypot-защитой на нескольких страницах, мобильное меню — на desktop и mobile
вьюпортах.

Что осталось клиенту: заполнить плейсхолдеры (`CONTENT-TODO.md`), заменить SVG-заглушки на
реальные фото, подключить `{{FORM_ENDPOINT}}` и `{{METRIKA_ID}}`, заменить домен-плейсхолдер
`pro-remont76.ru`, прогнать Lighthouse перед сдачей (чек-лист в `README.md`).

Запуск для просмотра: `npm run dev` → http://localhost:4321
